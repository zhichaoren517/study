
module.exports = {
    //默认环境变量 NODE_ENV : production development
    // publicPath:process.env.NODE_ENV === 'production' ? "http://www.baidu.com" : "/",

    // assetsDir:"assets",//静态文件(css js img)直接输出在dist下,在dist下的assetsDir目录下
    // outputDir:"./mydist",//打包文件不叫dist 叫outputDir
    // productionSourcemap:false,//打包 不再使用sourcemap
}